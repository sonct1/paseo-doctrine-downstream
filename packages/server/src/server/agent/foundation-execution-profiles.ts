/**
 * COMPAT(slpPolicyExtraction): legacy import path only.
 * Canonical execution-profile semantics are owned by the bundled `slp` policy pack.
 */
export * from "../policy/bundled/slp/execution-profiles.js";
