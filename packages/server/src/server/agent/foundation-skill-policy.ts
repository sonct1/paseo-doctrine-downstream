/**
 * COMPAT(slpPolicyExtraction): provider adapters still consume this stable import path.
 * Canonical SLP skill admission is owned by the bundled `slp` policy pack.
 */
export * from "../policy/bundled/slp/skill-policy.js";
