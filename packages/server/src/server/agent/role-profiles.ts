/**
 * COMPAT(slpPolicyExtraction): legacy kernel/tests import path only.
 * Canonical SLP profile semantics and ceilings are owned by the bundled `slp` policy pack.
 */
export * from "../policy/bundled/slp/role-profiles.js";
