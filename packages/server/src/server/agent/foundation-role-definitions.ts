/**
 * COMPAT(slpPolicyExtraction): legacy import path only.
 * Canonical SLP role semantics are owned by the bundled `slp` policy pack.
 */
export * from "../policy/bundled/slp/role-definitions.js";
